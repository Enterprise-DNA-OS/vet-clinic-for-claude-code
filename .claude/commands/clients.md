---
description: Every active owner on one screen - patients, money owing, last visit.
---

1. Run `node scripts/vet.mjs clients --json` (`--all` includes former clients).
2. Present as a table sorted by name; flag anyone owing money or whose last visit is over a year old.
3. For one owner's whole story, use `/client`.
